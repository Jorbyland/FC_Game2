using System;
using UnityEngine;
using Main;

namespace Game
{
    public class GameController : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
            // Main.AppContext.A.Player.Setup();
            // Main.AppContext.A.Player.Init();
        }

    }
}
